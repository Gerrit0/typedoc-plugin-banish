{
  "name": "typedoc-plugin-banish",
  "version": "1.0.1",
  "main": "src/index.js",
  "keywords": [
    "typedocplugin"
  ],
  "license": "MIT",
  "peerDependencies": {
    "typedoc": ">= 0.19 && <= 1.0"
  },
  "devDependencies": {
    "@types/minimatch": "^3.0.3",
    "@types/node": "^14.6.3",
    "prettier": "^2.1.1",
    "typedoc": "^0.19.0",
    "typescript": "^4.0.2"
  },
  "scripts": {
    "build": "tsc",
    "build:test": "yarn build && tsc -p tsconfig.test.json",
    "test": "node test/plugin.test.js",
    "lint": "prettier --check ."
  },
  "dependencies": {
    "minimatch": "^3.0.4"
  },
  "files": [
    "src",
    "README.md"
  ]
}
